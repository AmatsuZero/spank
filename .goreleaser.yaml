version: 2

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/spank
    env:
      - CGO_ENABLED=0
      - GOPRIVATE=github.com/taigrr/apple-silicon-accelerometer
    goos:
      - darwin
    goarch:
      - arm64
    ldflags:
      - -s -w -X github.com/AmatsuZero/spank.version={{.Version}}

archives:
  - format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

release:
  github:
    owner: AmatsuZero
    name: spank
